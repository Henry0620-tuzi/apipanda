window.APIPANDA_CONFIG = Object.freeze({
  // 部署 Sub2API 后改为实际地址，例如：https://api.example.com/api/v1
  apiBaseUrl: '',
  // 网关地址通常是同一域名根路径，例如：https://api.example.com
  gatewayBaseUrl: '',
  requestTimeoutMs: 15000,
  enableLiveData: false
});
